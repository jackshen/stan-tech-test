import styled from "styled-components";

const StyledErrorText = styled.div`
  color: ${(props) => props.theme.palette.graniteGrey};
  font-size: 20px;
  font-weight: bold;
`;

export default StyledErrorText;
